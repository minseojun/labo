-- ============================================================================
-- LABO — 모듈 개인별 탭바 표시/숨김
--
-- 지금까지 모듈 켜고 끄기는 교수(랩 관리자)만 할 수 있었고, 랩 전체에 적용됐음.
-- 이제 랩이 이미 켠 모듈에 한해서, 구성원 각자가 "내 탭바에는 안 보이게" 개인적으로
-- 숨길 수 있게 함. 랩 설정이나 모듈 데이터는 전혀 건드리지 않고, 본인 화면
-- 표시 여부만 profiles.hidden_modules에 저장됨(RLS는 기존 본인 전용 정책 그대로 적용).
--
-- 몇 번을 실행해도 안전해요(idempotent). Supabase 대시보드 → SQL Editor →
-- New query → 이 파일 전체를 붙여넣고 Run.
-- ============================================================================

alter table profiles add column if not exists hidden_modules text[] not null default '{}';
