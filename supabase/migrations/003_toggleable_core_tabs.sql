-- ============================================================================
-- LABO — 기본 탭(일정/장비/타이머/소모품)을 랩별로 끌 수 있게 함
-- Supabase SQL Editor에서 실행하세요 (002_wet_lab_module.sql 다음 단계)
-- ============================================================================

-- 기본값 '{}' (빈 배열) = 전부 켜짐. 여기 탭 id가 들어가면 그 탭이 꺼짐.
alter table labs add column if not exists disabled_tabs text[] not null default '{}';
